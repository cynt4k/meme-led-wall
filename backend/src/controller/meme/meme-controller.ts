import { Request, Response, NextFunction } from 'express';

export namespace MemeController {

    export const postUploadMeme = (req: Request, res: Response, next: NextFunction) => {

        // TODO: Handle upload
    };
}
